---
name: anyllm-prime
description: |
  anyllm — Emit a copy-pasteable briefing for the next LLM
  Triggered when the user types $anyllm-prime.
---

Run the following shell command and display its output to the user:

```bash
anyllm prime $ARGUMENTS
```

Do not add any extra commentary. Run the command and show the result.
