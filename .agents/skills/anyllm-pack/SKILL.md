---
name: anyllm-pack
description: |
  anyllm — Pack current session into .anyllm/current.md — no tokens used
  Triggered when the user types $anyllm-pack.
---

Run the following shell command and display its output to the user:

```bash
anyllm pack $ARGUMENTS
```

Do not add any extra commentary. Run the command and show the result.
