---
name: anyllm-diff
description: |
  anyllm — Show the snapshot of a single session
  Triggered when the user types $anyllm-diff.
---

Run the following shell command and display its output to the user:

```bash
anyllm diff $ARGUMENTS
```

Do not add any extra commentary. Run the command and show the result.
