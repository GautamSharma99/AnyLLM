---
name: anyllm-push
description: |
  anyllm — Paste briefing into target and press Send — silent
  Triggered when the user types $anyllm-push.
---

Run the following shell command and display its output to the user:

```bash
anyllm push $ARGUMENTS
```

Do not add any extra commentary. Run the command and show the result.
