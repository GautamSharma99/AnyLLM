---
name: anyllm-repack
description: |
  anyllm — Ingest turns missed since last pack and merge into current.md — no tokens used
  Triggered when the user types $anyllm-repack.
---

Run the following shell command and display its output to the user:

```bash
anyllm repack $ARGUMENTS
```

Do not add any extra commentary. Run the command and show the result.
